# 🤖 Файл конфигурации GUI
# GUI Configuration File

[gui]
# Размер окна: compact, normal, large
window_size = normal

# Тема интерфейса: blue, green, dark-blue
theme = blue

# Автосохранение токена
save_token = true

# Автоматический запуск бота при старте GUI
auto_start_bot = false

# Максимальное количество строк в логах
max_log_lines = 1000

# Интервал обновления статуса (секунды)
status_update_interval = 5

# Путь к файлу логов бота
bot_log_file = bot.log

# Позиция окна (x,y) - оставьте пустым для центрирования
window_position = 

[bot]
# Токен бота (будет перезаписан при сохранении через GUI)
token = 

# Режим отладки бота
debug = false

# Путь к исполняемому файлу Calibre
calibre_path = /usr/bin/ebook-convert

[advanced]
# Таймаут для проверки статуса бота (секунды)
bot_status_timeout = 3

# Автоматическая очистка логов при превышении лимита
auto_clear_logs = true

# Показывать системные уведомления
show_notifications = true
